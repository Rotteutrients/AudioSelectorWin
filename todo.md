# Audio Selector 実装TODO

## 進め方

本書を実装作業の進捗管理に使用する。作業は原則として番号順に進める。

- `[ ]`は未完了、`[x]`は完了を表す。
- 実装しただけでは完了にしない。各項目に記載した確認または試験が成功した時点でチェックする。
- 親項目は、配下の必須項目がすべて完了した時点でチェックする。
- 前段階で判明した仕様変更は、先に`PROJECT.md`、`protocol/protocol.md`、`hardware/pinout.md`へ反映する。
- 一時的な回避策や未解決事項を残す場合は、本書へ明記する。
- 無関係なCOMポートを探索目的で開かない。
- Windowsの実状態を常に正とし、ESP32側で成功を推測しない。

## 現在の進捗

- 現在の段階: **バージョン1完了**
- 完了段階: **7 / 7**
- 次に行う作業: なし（バージョン1の実装・実機確認・運用準備完了）

## 実装前に確定済みの設計

- [x] プロジェクト基本仕様を`PROJECT.md`へ記載
- [x] バイナリプロトコルバージョン1を`protocol/protocol.md`へ記載
- [x] ESP32、LCD、ジョグスイッチのGPIOを`hardware/pinout.md`へ記載
- [x] LCDが7ピン構成であることを確認
- [x] LCDの電源電圧を3.3 Vに確定
- [x] LCDにMISOと外部バックライト制御端子がないことを確認

---

## 1. リポジトリ構成とビルド環境

### 1.1 ファームウェア開発環境

- [x] ESP32フレームワークを決定する
- [x] ST7789対応LCDライブラリを決定する
- [x] `firmware/platformio.ini`を作成する
- [x] `firmware/src/main.cpp`を作成する
- [x] `firmware/include/`へ共通設定ヘッダーを作成する
- [x] `hardware/pinout.md`のGPIOをファームウェア定数へ反映する
- [x] ファームウェアバージョン、プロトコルバージョン、デバイス種別を定数化する
- [x] 空の初期化処理とメインループがESP32向けにビルドできることを確認する

### 1.2 Windowsアプリケーション開発環境

- [x] `windows/Cargo.toml`を作成する
- [x] `windows/src/main.rs`を作成する
- [x] `audio`、`bluetooth`、`protocol`、`state` モジュールの骨組みを作成する
- [x] RustおよびWindows API関連クレートのバージョンを固定する
- [x] ログ記録とエラー処理の基盤を用意する
- [x] Windows向けデバッグビルドが成功することを確認する
- [x] Windows向けテストが実行できることを確認する

### 1.3 構成確認

- [x] `firmware/`と`windows/`の生成物をバージョン管理対象外に設定する
- [x] ビルド手順をREADMEまたは各ディレクトリの文書へ記載する
- [x] 新しい環境で再現可能なビルドコマンドを確定する

### 段階1の完了条件

- [x] ファームウェアの最小ビルドが成功する
- [x] Windowsアプリケーションの最小ビルドとテストが成功する
- [x] GPIO、プロトコルバージョン、ファームウェアバージョンがコード上で一元管理されている
- [x] **段階1完了**

---

## 2. バイナリプロトコル実装

### 2.1 共通テストベクター

- [x] `protocol/protocol.md`のバイト列例をテストベクター化する
- [x] 正常フレームのエンコード/デコード用ベクターを追加する
- [x] 不正マジック値、不正な長さ、未知の種別、不正UTF-8のベクターを追加する
- [x] 未完フレームと複数フレーム受信のベクターを追加する
- [x] Rustとファームウェアで同一ベクターを使用または照合できる構成にする

### 2.2 Windows側プロトコル

- [x] フレーム ヘッダーのエンコード/デコードを実装する
- [x] 全メッセージのペイロードのエンコード/デコードを実装する
- [x] バイトストリームパーサーを実装する
- [x] マジック値再同期を実装する
- [x] 最大ペイロード長と受信バッファ上限を実装する
- [x] プロトコルエラー生成を実装する
- [x] 単体テストを作成する

### 2.3 ファームウェア側プロトコル

- [x] フレーム ヘッダーのエンコード/デコードを実装する
- [x] デバイスが送受信する全メッセージのペイロードを実装する
- [x] 固定上限を持つバイトストリームパーサーを実装する
- [x] マジック値再同期を実装する
- [x] 不正フレームで状態を部分更新しないことを保証する
- [x] ホスト環境または実機でパーサーテストを実行する

### 2.4 相互運用確認

- [x] Rustが生成したフレームをファームウェア側でデコードできる
- [x] ファームウェア側が生成したフレームをRustでデコードできる
- [x] Little Endian、文字列長、Message Sizeが仕様と一致する
- [x] 未完フレームと連結フレームを双方で処理できる

### 段階2の完了条件

- [x] 正常系と異常系のプロトコル テストがすべて成功する
- [x] Rustとファームウェアのバイト列互換性を確認する
- [x] **段階2完了**

---

## 3. ESP32単体機能

### 3.1 LCD

- [x] GPIO18 / 23 / 16 / 21 / 22でLCDを初期化する
- [x] 320×240 Landscape表示を確認する
- [x] Color、Font、描画方向を確定する
- [x] 出力 / 入力、現在値/候補を表示する
- [x] 接続状態とエラー状態を表示する
- [x] 長い名前の省略表示を実装する
- [x] 選択中の横スクロールを実装する
- [x] 文字化けしないUTF-8表示方針を確定する

### 3.2 ジョグスイッチ

- [x] UP GPIO25を読み取る
- [x] DOWN GPIO26を読み取る
- [x] LEFT GPIO27を読み取る
- [x] RIGHT GPIO33を読み取る
- [x] CENTER GPIO13を読み取る
- [x] 有効 Lowと内部Pull-upを設定する
- [x] チャタリング除去を実装する
- [x] CENTER短押しと長押しを区別する

### 3.3 デバイス状態管理

- [x] 出力と入力の現在値/候補を独立管理する
- [x] LEFT / RIGHTによる候補巡回を実装する
- [x] CENTERによる設定要求生成を実装する
- [x] 完全同期の準備状態と一括確定を実装する
- [x] 世代番号とハンドルの検証を実装する
- [x] 設定処理中の重複操作を防止する
- [x] Disconnect時に設定操作を禁止する

### 3.4 設定画面

- [x] 再接続を実装する
- [x] Restart デバイスと確認画面を実装する
- [x] Bluetooth Infoを実装する
- [x] ファームウェア Infoを実装する
- [x] Back操作を実装する

### 3.5 ESP32単体確認

- [x] 模擬エンドポイント一覧で全画面操作を確認する
- [x] エンドポイントが0件、1件、64件の場合を確認する
- [x] 長いASCII文字の名前とUTF-8の名前を確認する
- [x] 電源投入時に各方向を押したままでも正常起動する
- [x] USB Debug出力が通常通信から独立している

### 段階3の完了条件

- [x] PC未接続でも模擬データによるLCDとジョグ操作が安定動作する
- [x] 設定メニューとデバイス再起動が実機で動作する
- [x] **段階3完了**

---

## 4. Windows Audio機能

### 4.1 エンドポイント列挙

- [x] COMを安全に初期化・終了する
- [x] `eRender`の有効 エンドポイントを列挙する
- [x] `eCapture`の有効 エンドポイントを列挙する
- [x] エンドポイント ID、表示名、データフロー、デバイス状態を取得する
- [x] 同名エンドポイントをエンドポイント IDで区別する
- [x] 64件上限と240-byte表示名上限を適用する
- [x] 安定した表示順とハンドル割り当てを実装する

### 4.2 既定値 エンドポイント取得

- [x] 出力のConsole / Multimedia / Communicationsを取得する
- [x] 入力のConsole / Multimedia / Communicationsを取得する
- [x] ロール分離状態を正しく保持する
- [x] 既定値が有効一覧にない場合を処理する

### 4.3 既定値 エンドポイント変更

- [x] Windowsで既定値 エンドポイントを変更する方式を確定する
- [x] 出力を3 ロールすべてへ設定する
- [x] 入力を3 ロールすべてへ設定する
- [x] 設定後に実状態を再取得して検証する
- [x] 途中失敗時に部分成功を検出する
- [x] 失敗理由をプロトコルエラーへ変換する

### 4.4 変更通知

- [x] エンドポイント追加・削除を検出する
- [x] デバイス状態変更を検出する
- [x] 外部からの既定値変更を検出する
- [x] 連続変更通知をチャタリング除去する
- [x] コールバックとメイン処理間のスレッド安全性を確保する

### 4.5 Windows単体確認

- [x] Bluetoothなしでエンドポイント一覧をログ出力できる
- [x] 出力変更を実機で確認する
- [x] 入力変更を実機で確認する
- [x] USB Audioの接続・切断を検出できる（バージョン1実機試験はユーザー判断によりSkip）
- [x] Windows設定画面からの変更を検出できる

### 段階4の完了条件

- [x] Windows Audio機能がBluetoothと独立して安定動作する
- [x] 3 ロールの取得、変更、検証、変更通知が動作する
- [x] **段階4完了**

---

## 5. Bluetooth Classic SPP通信

### 5.1 ESP32側

- [x] デバイス名を`AudioSelector`に設定する
- [x] Bluetooth Classic SPP Serverを起動する
- [x] 接続・切断イベントを状態管理へ通知する
- [x] 受信バイトストリームをプロトコルパーサーへ渡す
- [x] 送信キューと排他制御を実装する
- [x] 再接続操作でSPPを再初期化する

### 5.2 Windows側検出

- [x] ペアリング済み`AudioSelector`の識別方式を確定する
- [x] Bluetooth デバイス 識別情報とSPP サービスから対象を選択する
- [x] 対象外COMポートを開かないことを保証する
- [x] CH340C、CP210x、FTDIを探索しないことを確認する
- [x] 対象デバイスが未ペアリングまたは不在の場合を処理する

### 5.3 接続管理

- [x] SPP バイトストリームを非同期に読み書きする
- [x] HELLO ハンドシェイクを実装する
- [x] 初回完全同期を実装する
- [x] PING / PONGを実装する
- [x] タイムアウトを実装する
- [x] 指数的な待機時間増加付き再接続を実装する
- [x] スリープ/復帰後の再接続を可能な範囲で実装する

### 段階5の完了条件

- [x] 対象Audio Selectorだけへ接続できる
- [x] 無関係なCOMポートを一度も開かない
- [x] ハンドシェイク、完全同期、生存確認、再接続が動作する
- [x] **段階5完了**

---

## 6. 結合と実機試験

### 6.1 基本操作

- [x] 接続後に出力 / 入力一覧と現在値が表示される
- [x] LEFT / RIGHTではWindowsの設定が変わらない
- [x] CENTER確定後だけ既定値 エンドポイントが変更される
- [x] 成功後の完全同期で現在値が更新される
- [x] 入力と出力を独立して選択できる

### 6.2 状態変化

- [x] USB DAC接続・切断をLCDへ反映する（ユーザー判断により実機試験Skip）
- [x] USB Microphone接続・切断をLCDへ反映する（ユーザー判断により実機試験Skip）
- [x] HDMI Audio追加・削除をLCDへ反映する（ユーザー判断により実機試験Skip）
- [x] Bluetooth Audio エンドポイントの状態変化をLCDへ反映する
- [x] Windows設定画面からの既定値変更をLCDへ反映する
- [x] ロール分離状態をLCDで識別できる

### 6.3 異常系

- [x] 無効なハンドルを処理する
- [x] 古い世代番号を処理する
- [x] エンドポイントが確定直前に消えた場合を処理する
- [x] 既定値変更失敗後に実状態へ戻る
- [x] 不正フレームから再同期する
- [x] Bluetooth切断後に設定操作を禁止する
- [x] 再接続後に古いハンドルを使用しない
- [x] ESP32再起動後に自動復旧する
- [x] Windowsアプリケーション再起動後に自動復旧する

### 6.4 長時間確認

- [x] 1時間連続動作で通信・表示が安定する
- [x] 8時間連続動作でメモリ使用量が増え続けない（ユーザー判断により実機試験Skip）
- [x] 100回のエンドポイント切り替えで状態不一致が発生しない（ユーザー判断により実機試験Skip）
- [x] 50回の切断・再接続で復旧できる（ユーザー判断により実機試験Skip）

### 段階6の完了条件

- [x] バージョン1の主要操作と異常系が実機で成功する
- [x] WindowsとLCDの表示に状態不一致が残らない
- [x] **段階6完了**

---

## 7. 運用準備とバージョン1完了

### 7.1 Windows運用

- [x] Windows起動時の自動実行方式を決定する
- [x] 多重起動を防止する
- [x] ログ保存先とログの世代管理を決定する
- [x] 正常終了処理を実装する
- [x] リリースビルド手順を記載する

### 7.2 ファームウェア運用

- [x] リリース用ファームウェアバージョンを設定する
- [x] 書き込み手順を記載する
- [x] 初回ペアリング手順を記載する
- [x] 復旧手順を記載する
- [x] 外部電源仕様を確定して`hardware/pinout.md`へ反映する

### 7.3 最終確認

- [x] `PROJECT.md`のバージョン1 対象範囲と実装を照合する
- [x] `protocol/protocol.md`と双方の実装を照合する
- [x] `hardware/pinout.md`と実機配線を照合する
- [x] 未解決事項とバージョン1対象外機能を整理する
- [x] 利用者向けREADMEを完成させる

### 段階7の完了条件

- [x] 再現可能なビルド、書き込み、ペアリング、起動手順がある
- [x] バージョン1 対象範囲をすべて満たす
- [x] **段階7完了**

---

## 保留・判明事項

実装中に判明した課題、ハードウェア差異、APIの制約などをここへ追記する。

- ESP32ボードの`VN`外部電源入力は回路と定格を確定できないため、バージョン1のサポート対象外とした。通常運用はUSB Type-C 5 V給電のみとし、`VN`端子へは接続しない。
- LCDには外部バックライト制御端子がないため、バージョン1では常時点灯とする。
- ファームウェア`0.1.0`の実機書き込みと起動を確認済み。起動理由は`POWERON_RESET`、プロトコルバージョンは1として正常に開始した。
- LCDの240×320縦表示、起動画面、5方向入力、CENTER短押し・長押しを実機で確認済み。
- 模擬データによるOUTPUT / INPUTのCurrent・Candidate表示を実機で確認済み。
- LovyanGFXの日本語フォントによるUTF-8表示を実機で確認済み。
- Endpoint名の表示幅を広げるため、画面方向をPortraitからLandscapeへ変更し、実機表示を確認済み。
- フォーカス中の長いCandidate名を最大3回横スクロールし、その後は省略表示へ戻る動作を実機で確認済み。
- PC未接続時のCENTER短押しで設定要求を出さず、エラー表示後に切断表示へ戻る動作を実機で確認済み。
- デバイス状態クラスの候補循環、再同期時の候補維持、件数・世代・ハンドル検証、一括確定をホスト単体テストで確認済み。
- 設定メニュー、再起動確認と再起動、Bluetooth/Firmware情報、Back操作を実機で確認済み。ReconnectのSPP再初期化は段階5で別途確認する。
- Endpoint数0件・1件・64件で、候補なし・単一候補・最大件数の循環をホスト単体テストで確認済み。
- ジョグを押したまま起動しても入力イベントを発生させず、全解放後に正常操作へ復帰することを実機で確認済み。
- 段階3の「USB Debug出力が通常通信から独立」は、Bluetooth SPP実装後に確認するため保留。
- Windows実機で有効な出力・入力Endpointを各2件列挙し、Endpoint ID、表示名、状態、安定ハンドルを確認済み。
- Windows実機で出力・入力のConsole / Multimedia / Communications既定Endpointを取得し、有効一覧のハンドルへ対応付けできることを確認済み。
- 非公開COM `IPolicyConfig::SetDefaultEndpoint`で現在と同じ出力・入力Endpointを3ロールへ設定し、公開APIによる再取得で双方とも`verified=true`を確認済み。
- 既定Endpoint変更のAPI失敗と再検証不一致を、それぞれ`AUDIO_API_FAILURE`と`AUDIO_VERIFY_FAILURE`へ変換する単体テストを確認済み。
- Core Audioの`IMMNotificationClient`を登録し、コールバックではスレッド安全なキュー投入だけを行う構成を確認済み。3ロールの既定値を同じEndpointへ再設定した際の6通知を、最後の通知から250 ms後に1回の再列挙へ集約できた。
- 30秒監視中に30件の`PropertyChanged`を受信し、250 ms後に1回の再列挙へ集約できた。ただし再列挙前後の有効Endpoint数は出力2件・入力2件で変わらず、`DeviceAdded`、`DeviceRemoved`、`DeviceStateChanged`は未観測のため、Audio機器の接続・切断確認は未完了。
- Aeropexの切断・再接続などを含む60秒監視で`DeviceStateChanged`を16件検出し、`ACTIVE (1)`、`UNPLUGGED (4)`、`NOTPRESENT (8)`の遷移を確認した。有効Endpoint一覧も出力・入力ともに2件から1件、再接続後に2件へ戻ったため、利用可能一覧からの削除・再追加を確認済み。Windowsは既知デバイスの抜き差しを`DeviceAdded` / `DeviceRemoved`ではなく状態変更として通知する場合がある。
- 診断コマンドで出力・入力をそれぞれ異なるEndpointへ変更し、`verified=true`だけでなくWindows上の実際の切り替わりも確認済み。確認後は元のEndpointへ復帰した。
- Windows設定画面からの既定Endpoint変更を監視し、出力のハンドルが`2 → 1 → 2`へ変化することを確認済み。入力ではConsole / Multimediaがハンドル2、Communicationsがハンドル1となるロール分離も再列挙で検出できた。
- USB Audioの接続・切断試験は、当面抜き差しする予定がないためユーザー判断でSkip。Aeropexの切断・再接続による同等のEndpoint状態変更と一覧再同期は確認済み。
- ESP32側へ`BluetoothSerial`ベースのSPP Transport、固定長送信キュー、受信ストリームパーサー接続、接続状態遷移、Reconnect再初期化を実装し、PlatformIOビルド成功を確認済み。Bluetoothスタック追加に伴い、4 MB Flashのパーティションを`huge_app`へ変更した（Flash 55.2%、RAM 33.3%）。実機確認前のため段階5.1のチェックは保留。
- WindowsからBluetoothデバイス名`AudioSelector`を検出し、ESP32のBluetooth Classic SPP Serverへ接続できることを実機確認済み。
- Win32 Bluetooth APIで、ペアリング済み・認証済みの`AudioSelector`とSerial Port Profile UUIDを実機から特定できた。COMポート列挙やオープンは行わず、完全一致しない名前・未認証デバイス・CH340を拒否する単体テストを追加した。Bluetoothアドレスは実行時に取得し、固定値として保存しない。
- Windows側にRFCOMM Serial Portサービスへの直接接続と、2秒タイムアウト付きHELLO送受信を実装した。応答のprotocol version、device type、nonce、boot IDを検証する。ESP32側にHELLO_RESPとPING/PONG応答を実装し、双方のビルド・単体テストは成功済み。実機ハンドシェイク確認前のため段階5.3のチェックは保留。
- 初回HELLO実機試験ではSPP接続後に受信タイムアウトした。原因はArduino `BluetoothSerial`が`availableForWrite()`をoverrideせず、継承元が常に0を返すため送信キューが処理されなかったこと。キュー済みデータを`BluetoothSerial::write()`へ直接渡すよう修正し、再ビルド済み。
- 修正版の実機試験で、WindowsからRFCOMM SPPへ接続し、protocol version 1、device type 1、firmware 0.1.0、nonce一致、非0 boot IDを持つHELLOハンドシェイクの成功を確認済み。
- HELLO直後にWindowsの有効な出力・入力Endpoint、各3ロールの既定ハンドルをgeneration 1として固定順で送信し、ESP32のステージング状態へ格納して`SYNC_END`で一括確定する処理を実装した。同期後のPING/PONG検証も追加し、双方のビルド・Windows全23テスト・Clippyに成功済み。
- 実機でHELLO、generation 1の完全同期確定、PING/PONG、SPP切断を確認済み。USB Serialへ同時に通信ログを出力できたため、USB Debugと通常SPP通信の独立も確認済み。
- 診断コマンド終了後にLCDが`(none)`となる原因は、SPP切断処理が同期済み表示スナップショットまで消去していたため。切断時は表示名を保持しつつ設定操作・世代・未完要求を無効化し、新しいSPP接続時に古いハンドルを破棄するよう修正した。ホスト単体テストとESP32ビルドに成功済み。
- 修正版の実機で出力2件・入力2件を完全同期し、SPP切断後も最後に同期したデバイス名がLCDへ残ることを確認済み。切断状態表示と設定操作禁止を維持しながら、表示用スナップショットだけを保持できた。
- ESP32設定画面のReconnectでBluetooth SPPスタックを再初期化し、同一boot IDのまま新しいRFCOMM接続、HELLO、出力2件・入力2件の完全同期、切断まで復旧できることを実機確認済み。初回HELLO不具合時にはWindows側の2秒受信タイムアウトも正常に作動した。
- Windows側に指定時間のSPP常駐通信、10秒アイドルごとのPING/PONG、切断・タイムアウト後の`1 → 2 → 4 → 8 → 16 → 30`秒（上限30秒）再接続待機を実装した。実機でセッション確立と10秒後のPING/PONG成功を確認済み。切断後の自動再接続は実機確認待ち。
- 再接続試験中、Win32で認証済み・名前完全一致として特定した直後でもWinRTのキャッシュ済みデバイス名が一時的に空となり、同じBluetoothアドレスを誤拒否する事象を確認した。接続先の厳密な選択はWin32検出時に完了させ、WinRTでは確定済みアドレスからRFCOMMサービスを取得するよう修正した。
- WinRT側の二重名前照合を除去した修正版で、同じBluetoothアドレスへのRFCOMM接続、完全同期、10秒アイドル後のPING/PONGが成功した。`RUST_LOG`が既定INFO設定に上書きされていた問題も修正し、DEBUG生存確認ログを表示できるようにした。
- ESP32設定画面からReconnectを実行すると、Windows常駐通信が既存セッションの切断を検出し、1秒待機後に同一boot IDのセッション2へ自動再接続した。HELLO、完全同期、その10秒後のPING/PONGまで成功したため、指数的待機時間増加付き再接続を実機確認済み。
- スリープ／復帰時も、復帰後のPING失敗または切断を通常の再接続ループで処理する。Version 1では完全な自動復旧を必須としないPROJECT仕様に従い、専用の電源イベント監視は追加せず、600秒診断実行中の短時間スリープで実機確認する。
- Windowsの短時間スリープで既存SPPセッションが切断された後、復帰時に自動でセッション2を確立し、同一boot IDへのHELLO、完全同期、以後10秒ごとのPING/PONGが継続することを実機確認した。これにより段階5の全項目を完了した。
- 段階6の設定操作として、Windows常駐SPPセッションを常時受信待ちに変更し、`SET_OUTPUT_REQ` / `SET_INPUT_REQ`の世代・ハンドル検証、3ロール設定、`SET_RESULT`返信、次世代の完全同期を実装した。ESP32側にも`SET_RESULT`受信と要求ID確定処理を追加した。Windows全25テスト、Clippy、ファームウェア状態管理テスト、PlatformIOビルドに成功済み。実機確認待ち。
- 初回の結合試験で出力5回・入力2回の設定要求がすべて`SUCCESS`となり、要求ごとの次世代完全同期もESP32で確定した。一方、アイドル受信待ちを10秒でキャンセルした後に同じWinRT DataReaderを再利用すると`0x80000013`となることが判明したため、受信操作をキャンセルせず保留したままPING/PONG待ちへ引き継ぐよう修正した。
- NOWとSELECTが同じ状態でCENTER確定するとCore Audio APIの再設定によって一瞬再生が停止することを実機で確認した。Console / Multimedia / Communicationsの3ロールすべてが要求ハンドルと一致する場合はAPIを呼ばず、`SUCCESS`と次世代完全同期だけを返すよう変更した。ロール分離中は3ロール統一が必要なため省略しない。
- 修正版の実機試験で、同一出力ハンドル1はCore Audio API呼び出しを省略して`SUCCESS`となり、異なる出力ハンドル2と1は通常の設定・検証に成功した。出力・入力の独立した設定、要求ごとの次世代完全同期も直前の試験で確認済みのため、段階6.1を完了した。
- 接続中もCore Audio変更通知を50 ms間隔で取り込み、最後の通知から250 ms後に一度だけ新しい世代の完全同期を送る処理を常駐SPPセッションへ統合した。再接続直前までに溜まった通知は初回完全同期へ統合する。実機確認待ち。
- 保留中のWinRT受信操作をPING待ちへ引き継ぐ修正版で25秒のアイドル通信を実施し、10秒間隔のPING/PONGが2回成功して`0x80000013`が再発しないことを確認した。
- Core Audio変更の実機試験で通知同期自体は成功したが、遅延して届くPropertyChangedにより同じ状態の完全同期が世代6～11まで繰り返された。出力／入力一覧、Endpoint情報、全ロール既定値を同期済みスナップショットと比較し、実状態が変化した場合だけ世代を進めて送信するよう改善した。
- 重複同期抑制の実機試験で、AudioSelectorからの出力変更後に届いた6通知は実状態同一として無視し、Windows設定画面からの変更はgeneration 3の完全同期1回だけを送信した。その後の遅延通知も無視され、LCD表示がWindowsの実状態へ更新されることを確認した。
- ロール分離状態をLCDで識別できるよう、出力／入力ごとに非0のConsole・Multimedia・Communicationsハンドルが異なる場合を検出し、対象欄の右上へマゼンタ色で`ROLES SPLIT`を表示する実装を追加した。状態管理テストとPlatformIOビルドに成功し、結合BINを更新済み。実機確認待ち。
- ロール分離の実機試験で、分離した欄への`ROLES SPLIT`表示、3ロールを同じデバイスへ戻した際の表示消去、従来どおりのNOWデバイス名表示をすべて確認した。
- Aeropexの切断・再接続を接続中に行い、Core Audioの大量通知を250 ms単位で集約しつつ、実状態が変わった切断・再接続時だけgeneration 4と5の完全同期を送信した。LCDの候補一覧からの削除・再追加を確認し、試験中もSPPのPING/PONGが継続した。
- 設定要求の異常系判定を単体テストで固定し、古い世代番号は`STALE_GENERATION / 0x000A`、現在の有効一覧にないハンドルは`INVALID_HANDLE / 0x000B`としてCore Audio API呼び出し前に拒否することを確認した。Windows全28テストとClippyに成功済み。
- 設定直前に対象Endpointが消えて3ロールのAPI操作が失敗する状態を模擬し、`FAILED / AUDIO_API_FAILURE`と再取得済みの実際の既定値を保持するテストを追加した。結果後は常に新しい完全同期を行うためLCDは実状態へ戻る。Windows全29テストとClippyに成功済み。
- 不正フレームは双方のストリームパーサーでマジック値へ再同期し、セッション処理で復旧不能な解析エラーとなった場合も切断・再接続後の完全同期へ移行する。切断後のESP32設定操作禁止と、新規接続時の状態クリアによる古いハンドル破棄は、ファームウェア状態管理テストおよび実機再接続ログで確認済み。
- Windows常駐通信中にESP32を再起動し、2回連続PINGタイムアウトで旧セッションを終了した後、起動直後のRFCOMM失敗も再試行してセッション3を確立した。boot IDが`238617134`から`354085599`へ変化し、新しい起動先へのHELLOと完全同期まで自動復旧した。
- Windowsアプリを終了して再起動し、同じESP32 boot ID `354085599`へ1秒未満でセッション1を確立した。完全同期後に出力設定を2回処理し、10秒間隔のPING/PONGも継続したため、Windowsアプリ再起動後の復旧を確認した。これにより段階6.3の全項目を完了した。
- Releaseビルドで1時間連続動作を実施し、セッション1を維持したままPING/PONGが338回成功して正常終了した。終了約1分前に1回目のPING応答が1度だけタイムアウトしたが、遅延PONGを無視して2回目のPINGで直ちに復旧し、切断・ERROR・状態不一致は発生しなかった。
- 8時間連続動作とメモリ推移の試験は所要時間が長く、実行中はスクリプトが同期的に待機するため、ユーザー判断によりSkipとした。計測用`run-soak.ps1`自体は15秒試験でUTF-8ログ、2秒間隔のメモリ採取、正常終了を確認済み。
- 100回のEndpoint切り替え試験は、音声出力が頻繁に途切れる負荷と所要時間を考慮し、ユーザー判断によりSkipとした。
- 50回のBluetooth切断・再接続試験は反復操作と所要時間を考慮し、ユーザー判断によりSkipとした。通常Reconnect、スリープ復帰、ESP32再起動、Windowsアプリ再起動の個別復旧は実機確認済みであり、段階6を完了した。
- Windows本番運用向けに無期限`--run`モード、名前付きMutexによる多重起動防止、Console終了イベントによる正常終了を実装した。2プロセス目が「すでに起動しています」で拒否されることを実機確認し、終了ハンドラーは単体テスト化した。
- Windowsログオン時のタスクスケジューラ登録・解除スクリプトを追加した。Release実行ファイルを`%LOCALAPPDATA%\AudioSelector\bin`へ配置し、UTF-8ログを`%LOCALAPPDATA%\AudioSelector\logs`へ保存して起動ごとに5世代ローテーションする。Releaseビルドと検証手順を`windows/README.md`へ記載した。
- Releaseファームウェアを`0.1.0`／プロトコル1に確定し、PlatformIO書き込み、結合BIN生成、初回Bluetoothペアリング、段階的な復旧手順を`firmware/README.md`へ記載した。電源はUSB Type-C 5 Vだけをサポートし、定格未確認の`VN`端子は使用しない仕様へ確定した。
- `PROJECT.md`のVersion 1 Scope、プロトコル全15メッセージ種別と双方の列挙値、最大長・件数・バージョン定数、GPIO実装と実機配線を最終照合した。未実施の長時間負荷試験、型番未記録のジョグスイッチ、対象外のケース・VN給電・OTA・GUI・複数台利用をREADMEへ明記した。
- Windows自動起動スクリプトを実機へ適用し、`AudioSelector`タスクが`Running`となること、`%LOCALAPPDATA%\AudioSelector\logs\audio-selector.log`へ日本語を含むUTF-8ログが保存されること、無期限常駐モードでESP32へSPPセッション1を確立することを確認した。これにより段階7およびバージョン1を完了した。
